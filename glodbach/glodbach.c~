#include <stfio.h>

bool isPrime( int a ){
	int i;
	bool prime = true;

	for ( i = 2; i < a; ++i ){
		if (a % i == 0 && a =! i){
			prime = false;
			break;
		}
	}
	return prime;
}
main(){
	FILE *in = fopen(in, "glodbach.in", "r");
	FILE *out = fopen(out, "glodbach.out", "w");
	int n;
	int a = 2, b = 2;
	int i;
	bool done = false;

	fscanf(in, "%d", &n);
	
	b = n - 1;

	while(done =! true && 
return 0;
}
